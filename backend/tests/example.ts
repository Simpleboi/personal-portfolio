
// import { argv } from "node:process";

// argv.forEach((val, index) => {
//     console.log(`${index}: ${val}`);
// })

// // this returns the path to the file we're currently running
// console.log(argv[1]);
